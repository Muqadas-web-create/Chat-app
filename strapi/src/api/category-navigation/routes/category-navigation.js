'use strict';

/**
 * category-navigation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::category-navigation.category-navigation');
